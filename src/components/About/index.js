import React from "react";

const About = () => (
  <>
    <h1>Weather App</h1>
    <p class="lead text-muted">Web-based app for getting weather info.</p>
    <p class="lead text-muted">Just type the city.</p>
  </>
);

export default About;
